data: |
  object = { hello: "world" }
  test = [1, 2]
  range = 0x1..0x9
  regex = r/^\/test/
  block = ///
    ^test # comment
  ///
  regex == block

compiled: |
  var object, test, range, regex, block;
  object = { "hello": "world" };
  test = [1, 2];
  range = [0x1, 0x2, 0x3, 0x4, 0x5, 0x6, 0x7, 0x8, 0x9];
  regex = /^\/test/;
  block = /^test/;
  regex === block;
