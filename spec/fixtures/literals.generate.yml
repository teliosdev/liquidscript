data: |
  object = { hello: "world" }
  test = [1, 2]
  range = [1..9]
  regex = r/^\/test/
  block = ///
    ^test # comment
  ///
  regex == block

compiled: |
  var object, test, range, regex, block;
  object = { "hello": "world" };
  test = [1, 2];
  range = [1, 2, 3, 4, 5, 6, 7, 8, 9];
  regex = /^\/test/;
  block = /^test/;
  regex === block;
