data: |
  console = 2
  some_function = ()-> {
    console = 3
    test = 0..console
  }

compiled: |
  var some_function;
  console = 2;

  some_function = function() {
    var test;
    console = 3;
    return test = (function(a, b) {
      var out, i, t;
      out = [];
      if(a > b) {
        t = a; a = b; b = t;
      }
      for(i = a; i <= b; i++) {
        out.push(i);
      };
      return t === undefined ? out : out.reverse();
    })(0, console);
  };
