data: |
  console = 2
  some_function = ()-> {
    console = 3
    test = 0..console
  }

compiled: |
  var some_function;
  console = 2;

  some_function = function() {
    var test;
    console = 3;
    test = (function(a, b) {
      var out, i;
      out = [];
      if(a > b) {
        t = a; a = b; b = t;
      }
      for(i = a; i <= b; i++) {
        out.push(i);
      };
      return t === undefined ? out : out.reversed();
    })(0, console);
  };
