data: |
  nodejs = -> {
    out = false

    if(typeof global != 'undefined) {
      out = true
    }

    try {
      console.ummm()
    } catch(e) {}

    return out
  }

compiled: |
  var nodejs;

  nodejs = function() {
    var out;
    out = false;
    if(typeof global !== 'undefined') {
      out = true;
    };

    try {
      console.ummm();
    } catch(e) {};

    return out;
  };
