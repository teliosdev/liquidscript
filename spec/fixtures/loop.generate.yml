data: |
  t = 0x5

  while(t > 0) {
    t--
  }

  for(t = 0, t < 5, t += 1) {
    console.log(t)
  }

compiled: |
  var t;
  t = 0x5;
  while(t > 0) { t--; };
  for(t = 0; t < 5; t += 1) { console.log(t); };
